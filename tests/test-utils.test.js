/*import { describe, test, expect } from "vitest";
import { cloneTemplate } from "../scripts/utils.js";

describe("cloneTemplate", () => {
  test("clona el contenido de un template", () => {
    document.body.innerHTML = `
      <template id="test-template">
        <article class="test-card"></article>
      </template>
    `;

    const datos = document.getElementById("test-template");
    const resultado = cloneTemplate(datos);

    expect(resultado.querySelector(".test-card")).not.toBeNull();
  });*/
